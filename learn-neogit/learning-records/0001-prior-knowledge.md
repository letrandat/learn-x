# 0001: Prior knowledge established — lazygit user with some git CLI

User's initial state, established in setup conversation:

- **Editor**: Neovim (new user, not yet comfortable with navigation)
- **Previous git tool**: Lazygit — used regularly, knows git concepts through its UI
- **Git CLI**: "Some CLI" — knows basic concepts (commit, push, pull, branch) from lazygit's panels, but not the CLI commands behind them
- **neogit**: Already installed, has a config they're comfortable modifying
- **Project type**: Both personal and team projects (needs the full workflow)

**Implications**:
- Can skip "what is git" / "why version control" lessons
- Must always pair neogit actions with their `git <command>` equivalents — that's the mission
- Basic Neovim navigation tips needed in lessons (opening files, `:wq`, moving between buffers)
- Lessons should compare with lazygit's approach occasionally to leverage existing mental models
