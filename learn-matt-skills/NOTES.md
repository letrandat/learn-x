# Notes

Working notes + Dat's teaching preferences.

## Preferences (observed)

- Communication style: telegraphic, min tokens, noun-phrases fine. (From global CLAUDE.md.)
- Likes **structured breakdowns**: by component, then deep-dives. Asked explicitly for:
  components → 3 configs → design detail → daily flow → 4 failure modes.
- Wants output as **HTML lessons** via the `teach` skill, not just chat text.

## Workspace conventions

- Lessons: `lessons/NNNN-dash-case.html`, link the shared `assets/styles.css` + `assets/quiz.js`.
- Reuse assets first; only add a new component when a second lesson would duplicate it.
- Reference docs (glossary) in `reference/`; keep glossary authoritative across lessons.
- Quiz answers: equal length, no formatting tells (per teach skill).

## TODO / threads

- Confirm MISSION open question (use-as-is vs author-own) — drives Lesson 3 choice.
- Offer a hands-on "run setup on a real repo" session.
